import React, { useEffect, useState } from 'react';
import { ArrowLeft, UserCog } from 'lucide-react';
import { Link } from 'react-router-dom';
import { createPageUrl } from '@/utils';
import { readAppSettings, writeAppSettings } from '@/lib/appSettings';
export default function AccountSettings() { const [settings, setSettings] = useState(readAppSettings()); useEffect(() => { writeAppSettings(settings); }, [settings]); return <div className="min-h-[calc(100vh-64px)] bg-[#060606] px-4 py-4"><div className="mx-auto max-w-md rounded-[30px] border border-white/10 bg-[#101010] p-5"><div className="mb-6 flex items-center gap-3"><Link to={createPageUrl('SettingsPage')} className="grid h-11 w-11 place-items-center rounded-2xl border border-white/10 bg-white/[0.04] text-stone-200"><ArrowLeft className="h-4 w-4" /></Link><div><h1 className="text-3xl font-black">Account</h1><p className="text-sm text-stone-500">Session basics for this device.</p></div></div><button type="button" onClick={() => setSettings((prev) => ({ ...prev, account: { ...prev.account, rememberMe: !prev.account.rememberMe } }))} className="flex w-full items-center justify-between rounded-2xl border border-white/8 bg-white/[0.03] px-4 py-4"><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-white/[0.04]"><UserCog className="h-4 w-4 text-red-400" /></div><div className="text-white font-medium">Remember my email on this device</div></div><div className={`h-7 w-12 rounded-full p-1 transition ${settings.account.rememberMe ? 'bg-emerald-500' : 'bg-white/10'}`}><div className={`h-5 w-5 rounded-full bg-white transition ${settings.account.rememberMe ? 'translate-x-5' : ''}`}></div></div></button></div></div>; }

